# `write-spec` Skill 设计 Handoff

## 目的

在一个没有原会话上下文的新 Agent 中，继续与用户讨论并最终实现 `write-spec` skill。

当前仍处于设计讨论阶段。不要立即创建 skill；先继续收敛设计。只有用户明确表示讨论完成并授权实施后，才能修改 skill 文件。

## 仓库与分支

- 仓库：`/Users/cosss/code/ai/xuan-skills`
- 工作分支：`dev-write-spec`
- 分支起点：`6e946f9`（原分支 `matt-2ab958093e8`）
- 仓库规则：阅读并遵守根目录 `AGENTS.md`（链接到 `CLAUDE.md`）和 `CONTEXT.md`。

本 handoff 是该分支目前唯一预期变更。开始后续工作前先运行 `git status --short --branch`，保留用户或其他 Agent 的已有改动。

## 背景

用户在 Mobile Coder 仓库中进行了一次很长的灵畿登录 spec 设计会话。该会话不只做领域建模，而是完成了以下实践流程：

```text
阅读代码与测试
→ 还原当前行为
→ 划分领域和文档职责
→ 按决策分支持续追问
→ 每项给出 Agent 推荐和理由
→ 记录事实、背景、用户决定和覆盖关系
→ 区分目标模型、当前映射、现状偏差与演进方向
→ 审查消费者、UI、失败、并发、持久化等实践边界
→ 最终一致性审计
→ 用户授权后才写正式 spec
```

用户希望将这套方法抽取成可复用 skill，默认用于新功能、新需求，也支持为已有功能补建或演进 spec。

原始实践的完整决策记录位于：

- `/Users/cosss/code/joinai/mobile-coder/.scratch/lingji-authentication.md`

需要理解方法或查找具体案例时读取该文件，不要把其灵畿领域内容复制进通用 skill。

## 已确认的 `write-spec` 决定

### 定位

- skill 名称：`write-spec`，不使用 `xuan-` 前缀。
- user-invoked only。
- 放置目录：`skills/xuan/write-spec/`。
- 与现有 `xuan-to-prd` 无关；不要删除、替换、迁移或重新定义 `xuan-to-prd`。
- 默认面向准备建设的新功能或新需求。
- 同时支持：
  - 新功能；
  - 对已有功能提出新增或变更需求；
  - 为完整的已有功能补建 spec。
- 不能把 skill 描述成“从现有代码反向生成 spec”的专用工具。

### 目标产物

- 工作阶段维护决策记录。
- 讨论闭合且用户明确授权后，写一份或多份正式 spec。
- 文档数量和提纲由实际领域边界决定，不使用固定 PRD 模板。
- 对已有实现，spec 区分目标模型、当前实现映射、现状偏差、已有保障和演进验收。
- 对全新功能，不虚构当前实现；改为调查相邻模块、既有约束和可复用能力。
- 正式 spec 描述行为、边界和可验证结果，不默认锁死类名、函数、存储机制或实现步骤。
- 不默认包含任务拆分、负责人、排期或完整讨论历史。

### 假设与调查

- 能从代码、测试和已有文档确认的事实由 Agent 自行调查，不询问用户。
- 会改变规范行为、领域边界或演进方向的假设必须得到用户明确确认。
- 编辑性且不影响语义的决定可以由 Agent 处理。
- 不得把不确定事实写成用户已经确认的决定。

### 正式 spec 的目标（刚向用户提出，尚待用户反馈）

最近一次回复提出：spec 应成为功能或需求的长期行为真相源，让不了解讨论过程的人能够理解、实现、验证和演进该功能。

建议覆盖范围包括（按需使用，不是固定十四段模板）：

- 目标与问题；
- 范围与边界；
- 必需的术语和概念；
- 参与者与权威所有者；
- 正常功能行为；
- 状态、事件和生命周期；
- 逻辑接口与集成契约；
- 数据、身份、持久化和缓存；
- 失败与恢复；
- 时间、并发和多客户端；
- UI 或消费者的可观察行为；
- 当前实现映射，或全新功能的现有系统约束；
- 现状偏差与演进方向；
- 可验证的验收标准。

用户在看到这份说明后尚未评价，随后请求创建 handoff。新 Agent 应从询问用户对该目标和内容范围的看法继续，不要把它当成已经确认。

## 已确认的 Grilling 修改

目标文件：`skills/mattpocock-productivity/grilling/SKILL.md`

现有文件很短，当前强制一次只问一个问题且不维护文件。用户确认需要做以下增强：

### 批量提问

- 按同一主题和依赖关系组织一批问题，而非固定一次一个。
- 每项提供 Agent 推荐方案和理由。
- 代码或文档能回答的事实不要询问用户。
- 用户未回答的项不得默认为同意。

### 批次状态纪律

这是用户特别强调的规则：

- 当前批次没有全部解决前，不得提出下一批问题。
- 用户只回答部分问题时，只处理当前批次剩余项。
- 解释中发现的新分支归入当前批次补充，不开启下一批。
- “继续”不代表当前批次已经确认。
- 回答当前批次疑问时，结尾不得顺带提出下一批。
- 当前批次所有问题都得到明确答案后，Agent 可以自行宣布批次关闭并直接进入下一批，不需要再要求用户确认一次。
- 如果用户明确要求暂停或转换主题，即使当前批次已关闭也不得开始下一批。

### 可选决策记录

- 调用方提供记录路径时，Grilling 必须维护该记录。
- 用户要求记录但没有给路径时，默认 `.scratch/<topic>-decisions.md`。
- 普通 Grilling 没有记录要求时，不强制创建文件。
- `write-spec` 使用 Grilling 时始终启用决策记录。
- `write-spec` 负责确定主题和路径；Grilling 负责创建或继续维护文件。
- 已存在记录时，修改前必须重新完整读取，保留用户修改。
- 不自动删除最终决策记录。

决策记录不能只写最终答案，还应保留：

- 问题背景；
- 相关代码或文档事实；
- 待决定的分支；
- Agent 推荐及其原因；
- 用户决定及用户明确提供的原因；
- 对旧决定的覆盖和影响；
- 待确认或已确认状态。

不得替用户编造没有表达过的理由。Agent 推荐理由和用户理由必须分开。

## 已确认不创建的资源

用户明确否决为了该 skill 创建以下文件：

- `references/spec-audit-checklist.md`
- `references/spec-quality.md`
- `assets/decision-log-template.md`

原因：

- 决策分支发现是 Grilling 已经擅长的工作，不要用具体检查清单限制它。
- 一般 spec 质量常识没有足够价值成为单独 reference。
- 决策记录格式应直接由 Grilling 的核心指令定义。

因此当前预期的新 skill 结构只有：

```text
skills/xuan/write-spec/
├── SKILL.md
└── agents/
    └── openai.yaml
```

暂不需要 scripts、references 或 assets。

## 仓库约定待修改

用户已经确认：修改该仓库的规则，不再要求新建或增强的自有 skill 强制使用 `xuan-` 前缀。

可能涉及：

- `CLAUDE.md` / `AGENTS.md` 的交叉引用约定；
- `CONTEXT.md` 中“所有 skill 使用 xuan- 前缀”的表述；
- `README.md`；
- `skills/xuan/README.md`。

尚未讨论精确的新规则措辞。不要擅自批量修改；在设计讨论中确认是完全取消前缀语义，还是只取消强制要求并保留现有名称。

## 当前仍需讨论

先处理用户对“spec 的目标和内容”的反馈。当前没有开放的新批次；不要跳过该主题直接实现。

后续至少还需要收敛：

- `write-spec` 的完整阶段和每个阶段的停止条件；
- `write-spec` 与 Grilling 在 skill 指令层如何组合，避免重复职责；
- 正式 spec 路径的选择与项目约定优先级；
- 何时允许读取外部资料、制作原型或拆成多次会话；
- 对简单需求如何避免过度 Grilling；
- 最终一致性审计如何表达而不引入固定检查清单；
- `agents/openai.yaml` 的展示名称、短描述和默认提示；
- user-invoked only 在该仓库采用什么元数据形式；
- skill 实现后的验证与独立前向测试方式；
- 仓库前缀规则的最终替代文本。

不要把这份列表机械地一次全部提问。遵守 Grilling 的批次状态纪律，按依赖关系逐批讨论。

## 建议 Skills

- `skill-creator`：必须使用。先完整阅读 `/Users/cosss/.codex/skills/.system/skill-creator/SKILL.md`，按其创建、元数据生成和验证流程实施。
- `grilling`：继续设计讨论时使用，但遵守本 handoff 中已经确认、尚待落地的新批次纪律。
- `handoff`：只有需要再次跨会话转交时才使用。

不要使用领域建模 skill 来替代这次工作；用户已经明确区分了领域模型与更贴近实践的功能 spec 工作流。

## 实施时的约束

- 用户尚未授权实施。
- 授权前只讨论，不创建 `write-spec` 或修改 `grilling`、仓库规则。
- 授权后先重新检查工作区和本 handoff，保留其他改动。
- 新建 skill 时遵循 `skill-creator` 的初始化、`agents/openai.yaml` 生成和验证要求。
- 本仓库没有要求为该 skill 增加 README、DIFF、参考文档或其他辅助文件；不要自行增加无价值文件。
- 使用本仓库现有语言和格式习惯，但不要因为历史文档写着 `xuan-` 就违背用户已确认的新名称决定。
- 完成实现后至少验证 skill 目录结构和 frontmatter，并根据最终讨论决定是否进行独立前向测试。

