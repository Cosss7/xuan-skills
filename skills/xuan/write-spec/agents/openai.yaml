interface:
  display_name: "Write Spec"
  short_description: "Turn investigated decisions into durable specs"
  default_prompt: "Use $write-spec to investigate this feature, resolve its decisions, and write its spec."
policy:
  allow_implicit_invocation: false
